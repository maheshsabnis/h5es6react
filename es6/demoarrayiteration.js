let names = ["sean", "roger", "george", "trimothy", "piercce", "daniel"];

// the for..of loop that provides the enumerations for index based read from array
// this uses Iterators
for (let n of names) {
    console.log(n);
}